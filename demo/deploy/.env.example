# Copy to .env (gitignored). provision_cloudfront.py generates real values on first `up`
# if they are absent, and reuses whatever is here on re-runs so the edge stays stable.
ORIGIN_SHARED_SECRET=
EDGE_BASIC_AUTH_USER=demo
EDGE_BASIC_AUTH_PASS=password
